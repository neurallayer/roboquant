product: jvm/lib

description: roboquant ibkr support

apply:
  - ../project.module-template.yaml

repositories:
  - mavenLocal

dependencies:
  - ../roboquant
  - com.interactivebrokers:tws-api:10.19: compile-only
